extend type Query {
  regions: [Region] @all

  region(id: ID! @eq): Region @find
}

type Region {
  id: ID
  name: String
  label: String
  provinces: [Province!]! @hasMany
  projects: [Project!]! @belongsToMany
}
