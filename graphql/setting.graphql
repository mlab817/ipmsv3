type Setting {
	user: User @belongsTo
	compact: Boolean
	dark: Boolean
}

extend type Mutation {
	updateSetting(input: UpdateSettingInput @spread): User @field(resolver:"UpdateSetting") @guard(with:["api"])
}

input UpdateSettingInput {
	compact: Boolean
	dark: Boolean
}